language: bash

addons:
  apt:
    packages:
    - dia
    - inkscape
    - r-base-core
    - unoconv
    - libreoffice
    - poppler-utils
    - texlive-extra-utils

script:
    - bash tests/run-tests.sh

